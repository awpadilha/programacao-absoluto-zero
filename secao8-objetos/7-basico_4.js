const usuario = {
    email: 'aluno@cod3r.com.br'
};

usuario.nome = 'Aluno';
usuario.senha = '123465';

console.log(usuario.email);
console.log(usuario.nome);
console.log(usuario);

// Os atributos do objeto podem ser inseridos a qualquer momento na aplicação