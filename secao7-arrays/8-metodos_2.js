const numeros = [1, 2, 3, 4, 5, 8];

numeros.splice(1, 2); //retira numeros do array

console.log(numeros);

console.log(numeros.pop()); //remove o ultimo elemento do array, e também pode retornar-lo
console.log(numeros.pop());
console.log(numeros); 